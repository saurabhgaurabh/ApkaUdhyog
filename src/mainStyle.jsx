import { StyleSheet } from "react-native";

// import { StyleSheet } from "react-native/types_generated/index";


const style = StyleSheet.create({
    LoginMainContainer: {
        flex: 1,
        alignItems: 'center',
        backgroundColor: '#617beeff'
    },
    loginHeading: {
        fontSize: 54,
        fontWeight: 'bold',
        color: '#fff',
        backgroundColor: '#000',
        width: '100%',
        height: 200,
        textAlign: 'center',
    },
    LoginForm: {
        backgroundColor: '#fff',
        width: 'auto',
        height: 'auto',
    }
})

export default style;